let reduceArraySize=(arr)=>{
    // sort the array
    // iterate through the given array and count the elements
    var theSize = 0;
    // var newArray = [];
    for(let i=0; i<arr.length; i++){
        arr.sort();
        console.log(arr)
        theSize = arr.length
        // newArray.push(arr.length/2);
        // console.log(newArray)
        const reduce=()=>{
            
        }
     

    }
    console.log('array size is ' + theSize)

}
reduceArraySize([3,3,3,3,5,5,5,2,2,7])