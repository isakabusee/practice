let maxPoints=(elements)=>{
    // choose a value in the array that does not equal to v-1 or v+1 in the array
    // add points;
    // if a value has another value  v-1 or v+1 in the array delete both
    // point = 0;
    // if value = another value add both and the pairs and add points.
    // delete and remaining value and add points.


}